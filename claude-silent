#!/bin/bash

# Claude Code 静默启动脚本 - 禁用 Hook 通知

echo "🔕 Claude Code 启动 (通知已禁用)"
echo "使用 'CC_HOOKS_NOTIFY=on claude' 可启用通知"

# 启动 Claude Code 并传递所有参数（不设置环境变量，使用默认禁用）
claude "$@"